Generates tactics for a team playing a game of Capture the flag.

Uses influence maps evolved with a genetic algorithm.

Numpy is required, and pygame is optional (it's used to visualize the tactics).
